# The main pipeline for ci_cd_template
resources:
  pipelines:
    ci_cd_template_pipeline:
      name: ci_cd_template_pipeline
      ## Specify the 'catalog' field to configure this pipeline to make use of Unity Catalog:
      # catalog: catalog_name
      schema: ci_cd_template_${bundle.target}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
